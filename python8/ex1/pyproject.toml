[project] 
name = "ex1"
version = "0.1.0"
description = "my project python"
authors = [{name = "thay"}]
requires-python = ">=3.10"

dependencies = []

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"